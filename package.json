{
  "name": "com.example.parking",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^11.2.7",
    "@testing-library/user-event": "^12.8.3",
    "bootstrap": "^5.1.3",
    "cordova_script": "^1.0.0",
    "prop-types": "^15.7.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.3.0",
    "react-scripts": "4.0.3",
    "react-spring": "^9.3.0",
    "react-svg-pan-zoom": "^3.10.0",
    "react-svg-pan-zoom-loader": "^1.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "eject": "react-scripts eject",
    "reactstart": "react-scripts start",
    "initialize": "reco checkversion && react-scripts build && reco copy && cordova platform add browser",
    "before": "node before/browser.js",
    "start": "reco checkversion && react-scripts build && reco copy && reco serve",
    "build": "reco checkversion && react-scripts build && reco copy && cordova build browser",
    "start-android": "reco checkversion && react-scripts build -- --profile && reco copy && cordova run android",
    "build-android": "reco checkversion && react-scripts build -- --profile && reco copy && cordova build android",
    "start-ios": "reco checkversion && react-scripts build -- --profile && reco copy && cordova run ios",
    "build-ios": "reco checkversion && react-scripts build -- --profile && reco copy && cordova build ios"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "./",
  "displayName": "Parking",
  "description": "Приложение для поиска данных автомобиля на парковке по QR коду. Cordova + React",
  "main": "index.js",
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Maximum",
  "license": "Apache-2.0",
  "devDependencies": {
    "cordova-android": "^9.1.0",
    "cordova-browser": "^6.0.0",
    "cordova-plugin-dialogs": "^2.0.2",
    "cordova-plugin-qrscanner": "^3.0.1",
    "cordova-plugin-splashscreen": "^6.0.0",
    "cordova-plugin-whitelist": "^1.3.5"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {},
      "cordova-plugin-dialogs": {},
      "cordova-plugin-qrscanner": {},
      "cordova-plugin-splashscreen": {}
    },
    "platforms": [
      "browser",
      "android"
    ]
  }
}
