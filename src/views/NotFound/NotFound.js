import React, { Component } from 'react';


export const NotFound = (props) => {

    return (
        <div id={"NOT-FOUND"}>
            <main>
                {window.location.href}
            </main>
        </div>
    );
};
